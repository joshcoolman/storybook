# Playing with React, Typescript and Storybook

## Available Scripts

In the project directory, you can run:

#### `npm run start`
runs the react app

#### `npm run storybook`
runs the storybook app 



